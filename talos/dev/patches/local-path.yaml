machine:
  kubelet:
    extraMounts:
      - destination: /var/mnt/localpath
        type: bind
        source: /var/mnt/localpath
        options:
          - bind
          - rshared
          - rw
