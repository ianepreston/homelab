http:
  middlewares:
    chain-no-auth:
      chain:
        middlewares:
          - middlewares-rate-limit
          - middlewares-https-redirectscheme
          - middlewares-compress
 
    chain-basic-auth:
      chain:
        middlewares:
          - middlewares-basic-auth
          - middlewares-rate-limit
          - middlewares-https-redirectscheme
          - middlewares-compress