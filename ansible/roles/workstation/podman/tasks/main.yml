---

- name: Install podman and extensions
  become: true
  ansible.builtin.package:
    name: "{{ item }}"
    state: present
  with_items:
    - podman
    - podman-docker
    - netavark
    - aardvark-dns 
